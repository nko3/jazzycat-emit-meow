/*jshint expr:true*/
/*globals mapper, routes, router*/
describe('router', function () {
  it('mapper should exist', function () {
    mapper.should.exist;
  });

  it('routes should exist', function () {
    routes.should.exist;
  });

  it('router should exist', function () {
    router.should.exist;
  });
});